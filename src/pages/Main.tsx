import React from "react"
import Layout from "../components/ui/Layout"
import styles from "@styles/components/common.module.scss"

const Main = () => {
    return (
        <Layout>
            <section className={styles.common_wrapper}>
                <div className={styles.common_area}>
                    hello
                </div>
            </section>
        </Layout>
    )
}

export default Main
